# Скопируйте в .env и подставьте значения (файл .env в git не коммитить)

SUPABASE_URL=
SUPABASE_KEY=
BOT_TOKEN=
MOYK_API_KEY=

# Порт HTTP-сервера (webhook Express)
PORT=3000

# Публичный HTTPS-URL без завершающего слэша (для справки и curl setWebhook)
# PUBLIC_URL=https://bot.example.com
